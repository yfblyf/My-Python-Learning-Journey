# My Python Learning Journey 

欢迎来到我的 Python 学习之旅！这个仓库保存了我在学习 Python 过程中自主创造的各种代码项目。

---

## 📚 项目列表

### 🎂 破壳日计算器 (`1.1py`)

**功能描述**  
通过用户输入的出生年月日信息，计算该日期在一年中的第几天。

**实现思路**
1. 使用 `input()` 函数获取用户出生的年、月、日信息
2. 创建月份天数列表，预设每个月的初始天数
3. 判断是否为闰年，动态调整二月份的天数
4. 累加计算出生月份之前的总天数
5. 加上出生日的天数，输出最终结果

**核心代码逻辑**
```python
# 月份天数列表
months_days = [31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31]

# 闰年判断
if (year % 4 == 0 and year % 100 != 0) or (year % 400 == 0):
    months_days[1] = 29  # 闰年2月29天

# 计算总天数
total_days = sum(months_days[:month-1]) + day
```

## 🐍 学习心得与规划
这一次练习使用GitHub这个工具很新奇，首先我浏览了GitHub Trending中的Python分区，看到一个trendreader的项目，







